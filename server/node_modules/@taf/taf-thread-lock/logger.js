const logger = {};
module.exports = logger;
const tafLogs = require('@taf/taf-logs');

logger.timer = new tafLogs('TafDate', 'timer', { logTo: tafLogs.LogTo.Local });
logger.lock = new tafLogs('TafDate', 'lock', { logTo: tafLogs.LogTo.Local });
