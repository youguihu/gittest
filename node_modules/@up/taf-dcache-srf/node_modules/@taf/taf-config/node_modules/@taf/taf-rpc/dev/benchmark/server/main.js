var Taf   = require("../../../../taf").server;
var TRom  = require("./NodeJsCommImp.js").TRom;

var svr   = Taf.createServer(TRom.NodeJsCommImp);
svr.start({
    endpoint : "tcp -h 127.0.0.1 -p 14002 -t 10000",
    maxconns : 200000,
    protocol : "taf"
});
